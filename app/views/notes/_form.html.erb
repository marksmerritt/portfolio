<%= form_with model: [@notebook, @note] do |f| %>
  <%= f.text_field :title, id: "title-field" %>
  <%= f.text_area :body, id: "body-field" %>
  <%= f.file_field :screenshots, multiple: true, direct_upload: true, id: "screenshots-field" %>

  <%= f.submit "Save", id: "submit-btn" %>
<% end %>